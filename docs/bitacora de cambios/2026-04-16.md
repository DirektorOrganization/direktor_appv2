# Bitacora de Cambios — Sprint 2026-04-16 al 2026-04-19

Rama principal del sprint: `chore/resumen-y-hv7-ajustes-2026-04-18`

---

## 2026-04-17 — Commit `6496a74`
**"Adelanto de cambios en logica para control de hitos v7 y vista de diagrama como una pestaña mas"**

### Archivos modificados
- `assets/db/direktor_mobile_v2.sql` — ajuste de schema
- `lib/app/state/app_controller.dart` — nuevos metodos para HV7
- `lib/app/sync/background_sync_service.dart` — ajustes sync background
- `lib/data/app_repository.dart` — logica extendida para control de hitos
- `lib/data/app_repository_apply.dart` — ajustes apply mappers
- `lib/data/local/app_database.dart` — migracion/ajuste de tablas
- `lib/data/models/app_models.dart` — modelos actualizados
- `lib/features/control_hitos/presentation/screens/option_v7/hv7_screen.dart` — HV7 ampliado (Gantt como pestaña integrada, +1509 lineas)
- `lib/features/control_hitos/presentation/screens/hito_form_screen.dart` — rediseño parcial
- `lib/features/control_hitos/presentation/screens/hito_detail_screen.dart` — ajustes
- `lib/features/control_hitos/presentation/screens/option_v2/hv2_detail_screen.dart` — ajustes menores
- `pubspec.lock` — dependencias actualizadas

### Cambios clave
- Vista diagrama Gantt integrada como una pestaña mas dentro de HV7 (antes era pantalla separada)
- Logica de repositorio extendida para soportar el flujo completo del control de hitos v7
- Preparacion para eliminacion logica de hitos (`codEstado`)

---

## 2026-04-18 — Commits `6210beb`, `af91477`, `2af9f12`

### Commit `6210beb` — "chore: consolidar resumen cambios y ajustar flujo Control Hitos HV7"

#### Archivos modificados
- `android/gradle.properties` — desactivado Kotlin incremental (fix build Windows)
- `docs/doc appmobil/resumen cambios/` — reorganizacion de docs, nuevos resumenes
- `lib/app/router.dart` — eliminadas rutas deprecated (v2/v3/v5/v6)
- `lib/app/routes/route_names.dart` — eliminadas constantes deprecated
- `lib/features/control_hitos/presentation/screens/_deprecated/` — movidos option_v2, option_v3, option_v5, option_v6
- `lib/features/control_hitos/presentation/screens/hito_form_screen.dart` — rediseño visual completo
- `lib/features/control_hitos/presentation/screens/option_v7/hv7_screen.dart` — mejoras Gantt
- `lib/features/projects/presentation/screens/hub_default_screen.dart` — simplificado a 1 entrada Control de Hitos

#### Cambios clave
- **Promocion de HV7 como version final**: `RouteNames.controlHitos` → `Hv7Screen` directamente
- **Hub**: reemplazadas 7 entradas por 1: "Control de Hitos", `Icons.flag_rounded`, azul `0xFF0A66B7`
- **Deprecated**: v2/v3/v5/v6 movidos a `_deprecated/`, ya no se importan
- **hito_form_screen**: rediseño visual con paleta `_D`, secciones `_FormSection`/`_FormField`, `_DateTap`, `_ItemDropdownField`; seccion Penalidad condicional por `generalIsActive`
- **Gantt HV7**: divisiones de año como pill badge; linea de año nunca solapa con marcador de hito (`yearLineMinGap=16px`); INICIO/FIN lineas mas cortas que lineas de año
- **Android Kotlin fix**: `kotlin.incremental=false` en `gradle.properties` (fallo en Windows con PUB_CACHE en disco distinto al proyecto)

---

### Commit `af91477` — "Subimos cambios"

#### Archivos modificados
- `AGENTS.md` — creacion del archivo (99 lineas nuevas)
- `lib/app/router.dart` — ajuste menor
- `lib/app/routes/route_names.dart` — ajuste menor
- `lib/features/control_hitos/presentation/screens/hito_detail_screen.dart` — rediseño
- `lib/features/control_hitos/presentation/screens/option_v7/hv7_screen.dart` — fix menor

#### Cambios clave
- Creacion de `AGENTS.md` con documentacion tecnica del proyecto para agentes de IA
- Ajuste de hito_detail_screen (version previa al restore de codex/test1)

---

### Commit `2af9f12` — "Avances integracion de funcionalidad para eliminar hitos y agregarle nueva columa codEstado para manejo de eliminacion logica"

#### Archivos modificados
- `assets/db/direktor_mobile_v2.sql` — nueva columna `codEstado` en tabla de hitos
- `lib/data/app_repository.dart` — queries que consideran `codEstado` para filtrar eliminados
- `lib/data/app_repository_apply.dart` — mappers para `codEstado`
- `lib/data/app_repository_utils.dart` — helpers adicionales
- `lib/data/local/app_database.dart` — migracion para agregar columna `codEstado`
- `lib/features/control_hitos/presentation/screens/option_v7/hv7_screen.dart` — UI de eliminacion logica

#### Cambios clave
- Nueva columna `codEstado` en `conhit_detallehitos` para eliminacion logica (no se borra el registro, se marca)
- Hitos con `codEstado = 'E'` (o equivalente) se excluyen de las vistas operativas
- UI en HV7: opcion para eliminar hito con confirmacion

---

## 2026-04-19 — Cambios en sesion (sin commit propio aun)

### hito_detail_screen.dart — Restauracion + generalIsActive

**Motivacion**: el rediseño visual de hito_detail fue rechazado por el usuario. Se restauro el visual de la rama `codex/test1` y se agrego comportamiento condicional por `generalIsActive`.

#### Cambios aplicados
```dart
// En build():
final generalData = controller.milestoneGeneral;
final generalIsActive = generalData?.appliesToGeneral ?? false;

// Header: _TopPenaltyBadge solo si generalIsActive
if (generalIsActive)
  _TopPenaltyBadge(isPenalizable: item.isPenalizable, ...),

// Card detalles: fila Penalidad solo si generalIsActive
if (generalIsActive) ...[
  const _DividerGap(),
  _DetailRow(icon: Icons.payments_outlined, label: 'Penalidad', value: '...'),
],
```

#### Visual restaurado (codex/test1)
- Header: gradiente azul `[0xFF0A66B7, 0xFF0F7AD8]`, tags tipo/clasificacion, sync badge
- Cards: `_DetailRow` + `_DividerGap` con `AppTheme.brandBlue`
- Secciones: info del hito, ampliaciones, botones de accion

### AGENTS.md — Actualizacion
- Seccion Key quirks: nota sobre HV7 como version final y deprecated
- Seccion Docs: referencia a `docs/bitacora de cambios/`

### docs/bitacora de cambios/ — Creacion
- Nueva carpeta para bitacora cronologica de cambios por dia
- Este archivo es la primera entrada
